apiVersion: v1
kind: Secret
metadata:
  # FIX: Renamed from 'status-page-secrets' to 'app-secrets'
  # to match the reference in deployment.yaml (secretKeyRef: name: app-secrets)
  name: app-secrets
type: Opaque
stringData:
  db-password: "<DB_PASSWORD>"
  django-secret-key: "<DJANGO_SECRET_KEY>"
